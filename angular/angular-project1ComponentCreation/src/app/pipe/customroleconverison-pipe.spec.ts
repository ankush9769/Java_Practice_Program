import { CustomroleconverisonPipe } from './customroleconverison-pipe';

describe('CustomroleconverisonPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomroleconverisonPipe();
    expect(pipe).toBeTruthy();
  });
});
